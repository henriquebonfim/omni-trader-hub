# OmniTrader MVP Dependencies
# Note: CCXT 4.4.0+ added Lighter.AI exchange (lighter-client not on PyPI)
# GitHub installation has setuptools issues. Using stable PyPI version 4.3.x
ccxt>=4.0.0,<4.4.0
pandas>=2.0.0
# TODO(T25): pandas-ta is unmaintained (last release 2022). Consider ta-lib or manual indicators.
pandas-ta>=0.3.14b
pyyaml>=6.0
neo4j>=5.18
redis>=5.0.0
httpx>=0.25.0
python-dotenv>=1.0.0
structlog>=23.0.0

# Database migrations
sqlalchemy>=2.0.0

# Celery worker offloading (broker + result backend = existing Redis)
celery[redis]>=5.3.0

# API / Dashboard
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
websockets>=12.0
python-multipart>=0.0.9

# NOTE: For production deployments, use requirements.lock for reproducible builds.
# Generate with: pip freeze > requirements.lock
