# ============================================================================
# OmniTrader Development Dependencies
# ============================================================================
# Install with: pip install -r requirements.txt -r requirements-dev.txt
# ============================================================================

# First, include production dependencies
-r requirements.txt

# Testing Framework
# ----------------------------------------------------------------------------
pytest>=7.0.0,<10.0.0
pytest-asyncio>=0.21.0,<1.4.0

# Code Quality & Linting
# ----------------------------------------------------------------------------
# ruff: Fast Python linter & formatter (replaces flake8, black, isort)
ruff>=0.3.0,<1.0.0

# mypy: Static type checker
mypy>=1.0.0,<2.0.0

# Pre-commit: Git hooks for code quality
pre-commit>=3.6.0,<4.0.0

# Testing Infrastructure
# ----------------------------------------------------------------------------
# testcontainers: Spin up Docker containers for integration tests
testcontainers>=3.0.0,<5.0.0

# ============================================================================
# Development Workflow
# ============================================================================
# Setup:
#   pip install -r requirements-dev.txt
#   pre-commit install
#
# Run tests:
#   pytest tests/
#
# Type checking:
#   mypy src/
#
# Linting:
#   ruff check src/
#
# Formatting:
#   ruff format src/
# ============================================================================
