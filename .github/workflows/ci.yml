name: CI/CD

on:
  push:
    branches: ["main", "master"]
  pull_request:
    branches: ["main", "master"]

jobs:
  backend-test:
    name: Backend Tests & Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Install dependencies
        run: |
          cd backend
          python -m pip install --upgrade pip
          pip install uv
          uv pip install --system -r requirements.txt -r requirements-dev.txt

      - name: Lint with Ruff
        run: |
          cd backend
          ruff check .

      - name: Run tests
        env:
          POSTGRES_PASSWORD: omnitrader_password
        run: |
          echo "POSTGRES_PASSWORD=omnitrader_password" > .env
          cd backend
          POSTGRES_PASSWORD=omnitrader_password python -m pytest tests/ -v

  frontend-build:
    name: Frontend Build & Type Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: Install dependencies
        run: |
          cd frontend
          bun install

      - name: Build and Type Check
        run: |
          cd frontend
          bun run build
